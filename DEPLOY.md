# Инструкция по деплою бота

⚠️ **ВАЖНО:** Используйте GitHub Secrets или переменные окружения хостинга для хранения токенов. НИКОГДА не публикуйте токены в открытом виде!

## Вариант 1: Railway (Рекомендуется - самый простой)

### Шаги:

1. **Зарегистрируйтесь на Railway**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий (или создайте новый)

3. **Добавьте переменные окружения**
   - В настройках проекта найдите "Variables"
   - Добавьте переменные:
     - **Name:** `TELEGRAM_BOT_TOKEN` → **Value:** `ваш_токен_бота` (получите у @BotFather)
     - **Name:** `ADMIN_BOT_TOKEN` → **Value:** `ваш_токен_админ_бота` (если используете админ-бота)
     - **Name:** `ADMIN_IDS` → **Value:** `ваш_telegram_id` (получите у @userinfobot)

4. **Деплой**
   - Railway автоматически определит Python проект
   - Бот запустится автоматически

5. **Проверка**
   - В логах Railway должно быть "Бот запущен!"
   - Проверьте бота в Telegram

---

## Вариант 2: Render

### Шаги:

1. **Зарегистрируйтесь на Render**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройки:**
   - **Name:** `kraevedenie-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python3 bot.py`

4. **Добавьте переменные окружения**
   - В разделе "Environment Variables"
   - Добавьте:
     - **Key:** `TELEGRAM_BOT_TOKEN` → **Value:** `ваш_токен_бота`
     - **Key:** `ADMIN_BOT_TOKEN` → **Value:** `ваш_токен_админ_бота`
     - **Key:** `ADMIN_IDS` → **Value:** `ваш_telegram_id`

5. **Деплой**
   - Нажмите "Create Web Service"
   - Дождитесь завершения деплоя

---

## Вариант 3: Heroku (устарел, но работает)

### Шаги:

1. **Установите Heroku CLI**
   ```bash
   brew install heroku/brew/heroku
   ```

2. **Войдите в Heroku**
   ```bash
   heroku login
   ```

3. **Создайте приложение**
   ```bash
   heroku create kraevedenie-bot
   ```

4. **Добавьте переменные окружения**
   ```bash
   heroku config:set TELEGRAM_BOT_TOKEN=ваш_токен_бота
   heroku config:set ADMIN_BOT_TOKEN=ваш_токен_админ_бота
   heroku config:set ADMIN_IDS=ваш_telegram_id
   ```
   ⚠️ **Замените значения на реальные токены!**

5. **Деплой**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git push heroku main
   ```

---

## Вариант 4: Docker (для VPS или любого хостинга)

### Шаги:

1. **Соберите образ**
   ```bash
   docker build -t kraevedenie-bot .
   ```

2. **Запустите контейнер**
   ```bash
   docker run -d --name bot \
     -e TELEGRAM_BOT_TOKEN=ваш_токен_бота \
     -e ADMIN_BOT_TOKEN=ваш_токен_админ_бота \
     -e ADMIN_IDS=ваш_telegram_id \
     -v $(pwd)/data:/app/data \
     kraevedenie-bot
   ```
   ⚠️ **Замените значения на реальные токены!**

---

## Важные замечания:

⚠️ **Безопасность:**
- НИКОГДА не коммитьте файл `.env` в Git
- НИКОГДА не публикуйте токены в открытом доступе
- Используйте переменные окружения на хостинге
- Файл `.gitignore` уже настроен
- Используйте GitHub Secrets для хранения токенов (см. [GITHUB_SECRETS.md](GITHUB_SECRETS.md))

📝 **После деплоя:**
- Бот будет работать 24/7
- Ежедневная рассылка в 9:00 по времени сервера
- Проверьте часовой пояс на сервере

🔄 **Обновление:**
- После изменений в коде просто сделайте `git push`
- Платформа автоматически перезапустит бота

---

## Рекомендация:

**Используйте Railway** - это самый простой и бесплатный вариант для начала.
