# Telegram бот-адвент календарь для краеведения

Бот для ежедневной рассылки событий и заданий по краеведению.

## Возможности

- 📅 Ежедневная автоматическая рассылка событий в 9:00
- 🖼️ Поддержка текста, картинок и карт
- 📋 Просмотр всех событий
- 👥 Автоматическая подписка пользователей
- 🔐 Админ-бот для управления событиями и просмотра подписчиков

## Установка и запуск локально

1. **Клонируйте репозиторий**
   ```bash
   git clone <ваш-репозиторий>
   cd краеведение
   ```

2. **Установите зависимости**
   ```bash
   pip3 install -r requirements.txt
   ```

3. **Создайте файл `.env`**
   ```
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   ADMIN_BOT_TOKEN=ваш_токен_админ_бота
   ADMIN_IDS=ваш_telegram_id
   ```

4. **Запустите бота**
   ```bash
   python3 bot.py
   ```

## Админ-бот

Для управления событиями используйте админ-бота! 

**Подробная инструкция:** см. [ADMIN_SETUP.md](ADMIN_SETUP.md)

**Возможности админ-бота:**
- 👥 Просмотр всех подписчиков
- 📅 Просмотр всех событий
- ➕ Добавление событий (текст, картинки, карты)
- 🗑️ Удаление событий
- 📤 Тестовая рассылка

**Запуск админ-бота:**
```bash
python3 admin_bot.py
```

## Добавление событий

### Через админ-бота (рекомендуется)
Используйте админ-бота для удобного добавления событий через Telegram интерфейс.

### Вручную
Отредактируйте файл `data/events.json`:

```json
{
  "2024-12-15": {
    "title": "Название события",
    "description": "Описание события",
    "image": "https://ссылка_на_картинку.jpg",
    "map_url": "https://yandex.ru/maps/..."
  }
}
```

**Формат даты:** `YYYY-MM-DD`

## Деплой

См. подробную инструкцию в файле [DEPLOY.md](DEPLOY.md)

**Быстрый деплой на Railway:**
1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект из GitHub репозитория
3. Добавьте переменную `TELEGRAM_BOT_TOKEN`
4. Готово!

## Структура проекта

```
краеведение/
├── bot.py              # Основной файл бота
├── admin_bot.py        # Админ-бот для управления
├── database.py         # Работа с БД подписчиков
├── scheduler.py        # Планировщик рассылок
├── requirements.txt    # Зависимости
├── .env               # Токены ботов (не коммитится)
├── data/
│   └── events.json    # События и задания
└── subscribers.db     # База данных (создается автоматически)
```

## Команды бота

- `/start` - Начать работу с ботом и подписаться на рассылку

## Лицензия

MIT

